// a string in the student's own code can look just like a compiler message,
// and the line the compiler quotes back must not then be taken for one
int main(void) {
    printf("save.c:10: error: could not open file\n");
}
