
Runtime error: assigning to a value via a NULL pointer
dcc explanation: You are using a pointer which is NULL
  A common error is assigning to *p or p[index] when p == NULL.

Execution stopped in main() in struct_array_declaration.c at line 11:

int main(void) {
    struct s v[3];
    v[0].a = 1;
    int *p = 0;
--> *p = v[0].a;
    return 0;
}

Values when execution stopped:

p = NULL
v = {{a = 1}, {<uninitialized values>}, {<uninitialized values>}}
v[0] = {a = 1}

